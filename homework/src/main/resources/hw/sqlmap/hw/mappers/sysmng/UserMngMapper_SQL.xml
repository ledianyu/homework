<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hw.sysmng.service.impl.UserMngMapper">
		 
	<select id="selectLoginUserInfo" resultType="hw.sysmng.service.UserVO">
		SELECT USER_ID
			 , KNAME
			 , PASSWORD
			 , SCHOOL_CD
			 , GRADE_CD
			 , GROUP_ID
			 , STATE
		  FROM T_USER
		 WHERE USER_ID = #{loginUserId}
		   AND PASSWORD = #{logInPassword}
	</select>

</mapper>